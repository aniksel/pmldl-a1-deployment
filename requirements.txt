# Airflow needs its own constraints file, see README
apache-airflow==3.3.1

pandas==3.0.5
mlflow>=3.0,<4.0

# these versions come from the Airflow constraints and are repeated in
# code/deployment/api/requirements.txt: the model is trained here but
# loaded inside the container, so the versions must match
scikit-learn==1.9.0
numpy==2.5.1
joblib==1.5.3

# The API and the web app run inside Docker containers and install their own
# dependencies (FastAPI, uvicorn, pydantic / Streamlit, requests) from:
#   code/deployment/api/requirements.txt
#   code/deployment/app/requirements.txt
# They are not installed here: Airflow pins its own FastAPI version.
